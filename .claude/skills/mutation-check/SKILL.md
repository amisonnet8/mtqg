---
name: mutation-check
description: テストが本当に効いているかを、実装を1か所ずつ壊して確かめる（変異を入れ、テストが落ちるかを見て、必ず元に戻す）。新しいコマンド・検査・排他・出力の形・シェルのスクリプトを実装した区切りで使う。「壊して確かめる」「変異」「テストが効いているか」と言われたとき。
---

# 壊して確かめる（mutation-check）

通るだけのテストは、効いているとは限らない。**実装を1か所ずつ壊して、テストが落ちるかを見る。** 落ちなければ、そのテストはその約束を守っていない（`.claude/rules/testing.md`「並行・排他のテストは、壊して確かめる」）。Step 7・8・9で毎回この手順をその場で書いたので、ここに固定する。

手順は2段構え。**①`gen-mutants`で、構文だけで判定できる変異（比較演算子・論理演算子・算術演算子・真偽値リテラル・否定・境界値）を自動で総当たりする。** 見つかる変異は「仕様の言葉」ではなく「構文の言葉」（`==`を`!=`にする、など）なので、`gen-mutants`はこの段だけを引き受け、生き残ったものを人が読む。**②仕様の約束のうち、構文だけでは判定できないもの**（「終了日を含めない」「警告を出す」「ロックが待たない」のような、意味を読まないと分からない変異）は、これまでどおり人が一覧を書いて`mutate.sh`で1つずつ入れる。①だけで済ませず、②も必ず行う（①は「構文的に妥当な変更」を機械的に網羅するだけで、仕様の約束を読んでいない）。

## ①構文だけで判定できる変異：`gen-mutants`

`.claude/skills/mutation-check/gen-mutants/`は、mtqg本体とは別のGoモジュール（`go.mod`を持つ）。対象のリポジトリのビルド・lintの対象に入らないようにするため（`internal/`の一部ではなく、開発ツール）。

```bash
go build -o /tmp/gen-mutants ./.claude/skills/mutation-check/gen-mutants   # 一度だけ
/tmp/gen-mutants -C /workspaces/mtqg -file internal/cli/candidates.go -list   # 見つかる変異を一覧するだけ
/tmp/gen-mutants -C /workspaces/mtqg -file internal/cli/candidates.go -- \
  go test -count=1 ./internal/cli/ -run TestCandidates                        # 1つずつ入れて回す
```

- 見つける変異：比較演算子の反転（`==`↔`!=`、`<`↔`<=`、`>`↔`>=`）、論理演算子の反転（`&&`↔`||`）、算術演算子の反転（`+`↔`-`、`*`↔`/`）、インクリメント/デクリメントの反転（`++`↔`--`）、複合代入の反転（`+=`↔`-=`）、先頭の`!`の削除、真偽値リテラルの反転（`true`↔`false`）、整数リテラルの±1（境界値）
- 出力は`mutate.sh`と同じ語彙（`killed:`／`SURVIVED:`／`not compiled:`）。`not compiled:`は型が合わず意味を持たない変異なので、`mutate.sh`の終了コード3と同じく無視してよい
- **`-file`は1ファイルずつ**（変異は1つずつしか同時に入れない。1回の実行の中で全変異を順に試す）
- 生き残った変異の扱いは、下の②の4「生き残った変異（SURVIVED）を、握りつぶさずに読む」と同じ（`gen-mutants`が見つけたか、人が書いたかで扱いを変えない）
- **構文だけでは判定できない変異は見つけられない**（「新しい順に出す→古い順に出す」のような、演算子の反転では表せない変異は対象外）。②の手作業を代替しない

## ②仕様の言葉で書く変異：`mutate.sh`

1. **壊す約束の一覧を先に書く。** 仕様の約束1つにつき変異1つ。「終了日を含めない」「警告を出す」「ロックが待たない」のように、**仕様の言葉で**書く（コードの言葉にしない）。一覧は`testing.md`の該当項目に書いた変異と同じ形。
2. **1つずつ、`mutate.sh`で入れる。**

   ```bash
   bash .claude/skills/mutation-check/mutate.sh <名前> <ファイル> <元の文字列> <新しい文字列> -- <テストのコマンド>
   ```

   - **元の文字列は、ファイルに1回だけ出てくる、なるべく短い断片**にする（最初の1か所だけが置き換わる）。複数行でよい
   - 終了コード：`0`＝**killed**（テストが落ちた。効いている）、`1`＝**SURVIVED**（通った）、`2`＝使い方の誤り・置き換える文字列が無い・変異前からテストが落ちている、`3`＝**変異がコンパイルできない**（これは何も言っていない。変異を直す）
   - スクリプトは、走る前にファイルを退避し、**どう終わっても（テストの失敗、Ctrl-C、`kill`）元に戻す**。変異前にテストが通ることも先に確かめる（遅いときは`MUTATE_BASELINE=0`）
   - テストのコマンドは、その変異に関係するものだけ（`go test -count=1 ./internal/cli/ -run Candidate`、e2eなら`go test -tags e2e -count=1 -run '^TestX' ./e2e/`）。`-count=1`でキャッシュを避ける
   - 埋め込むスクリプト（`internal/cli/completions/`など）は、`go:embed`なので、e2eが建てるバイナリに入る。そのままファイルを壊せる

3. **一覧を回すときは、シェルのループで。** 終了コード1・3が出ても止まらないように`|| true`を付ける。

   ```bash
   m() { bash .claude/skills/mutation-check/mutate.sh "$@" || true; }
   T="go test -count=1 ./internal/cli/ -run Candidate"
   m "前方一致で絞らない" internal/cli/candidates.go '!strings.HasPrefix(cand.value, partial)' 'false' -- $T
   m "古い順に出す"       internal/cli/candidates.go 'slices.Backward(all)' 'slices.All(all)' -- $T
   ```

4. **生き残った変異（SURVIVED）を、握りつぶさずに読む。** 3つのどれかを判断する。
   - **変異が何も変えていない**（使われない変数を足しただけ、同じ意味に書き換えただけ）。変異を直して、もう一度。Step 9で実際にあった（`_ = all`を足しただけの「変異」が生き残った）
   - **テストが足りない。** その約束を確かめるテストを足し、**同じ変異をもう一度入れて、今度はkilledになる**ことを確かめる。足したテストが落ちることを見るまで、足したことにならない
   - **本当に同じ意味で、区別できない**（Step 9：fish 3.6では、`"--word=$cur"`の引用符を外しても動きが変わらない）。理由とともに`testing.md`に「生き残った変異」として書き、そのまま残す。**黙って消さない**

## 気をつけること

- **作業ツリーのソースを実際に書き換える。** 実行中は、同じファイルを編集しない。並べて走らせない（1つずつ）
- 終わったら`git status --short`で、意図しない差分が無いことを確かめる。コミットしていないファイルでも動く（スクリプトは`git`を使わず、退避したコピーで戻す）
- **変異は、gofmt・lintを通らなくてよい**（`qsoku check`は走らせない）。ただしコンパイルはできること（できなければ終了コード3）
- 時間のかかるテスト（`qsoku race`、全部のe2e）を、変異ごとに走らせない。変異に関係するテストだけを走らせる。最後に、変異を入れていない状態で、`qsoku check`・`qsoku test`を通す
- `PostToolUse`のフック（`.go`の編集後に`qsoku build`）は、`Edit`・`Write`のときだけ走る。`mutate.sh`が書き換えても走らない
- `gen-mutants`自身（`.claude/skills/mutation-check/gen-mutants/`）は独立したGoモジュールなので、対象リポジトリの`qsoku check`・`golangci-lint`には入らない。`gen-mutants`自体を変更したときは、そのディレクトリで`gofmt -l . && go vet ./... && go test ./...`を通す

## 記録する

- **壊して確かめた変異の一覧と、生き残ったもの**を、`.claude/rules/testing.md`の該当項目（「mtqg固有の検証項目」）と、`PLAN.md`の「手元で確かめたこと」に書く。**すべて検出した、とだけ書かない。**生き残ったものと、その扱い（テストを足した／同じ意味なので残した）を書く
