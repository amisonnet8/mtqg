# mtqg 設計文書

mtqg（memo / todo / qa / bug / glossary）の設計判断と、その理由の記録。日本語のみ（英語版は作らない）。bugは2026-09-21に足した5つ目の種類で、それ以前の文書は4種類の時点で書かれている（`history.md`）。

旧称：tdmqa（ToDoMemoQA）→ tmqg → mtqg。名前の由来と順序の意味は§3。

> **一文で言うと**
>
> - **作る側から見ると**：バージョン管理はgitがやる。mtqgは、gitに残らない過程を追記していくだけ。
> - **使う側から見ると**：理想は、いつもの体験はほぼ同じまま、記録がもっと細かく、リポジトリに残ること。
>
> 2つは意図的に異なる見え方にしている（§2.2）。判断に迷ったときは、内部の機能は前者に、使う側の機能は後者に照らす（§2.6）。

## 位置づけ

- ここにあるのは「**なぜこうなっているか**」の記録。**現在の仕様（何をするか）は `docs/reference/` が正**であり、食い違う場合は reference に従う
- 設計判断を変えるときは、まず `docs/reference/` を更新する。ここは経緯として残し、判断が大きく変わった場合だけ該当節に追記する（書き換えて過去の理由を消さない）
- 段階4（`PLAN.md`）以降、mtqg自身の経緯は `.mtqg/` に記録される。ここは「mtqgを使い始める前の設計の記録」という位置づけで、あまり増えない
- 計画・進捗・未決事項は `PLAN.md` にある

## 節番号

節番号は、旧「構想メモ」の章立てをそのまま引き継いだ**設計文書全体の通し番号**。本文中の「§7.4」「（7.4）」はファイルをまたいで一意に指せる。CLIの設計だけは別の番号体系で、「CLI設計§4.5」のように書く。

| ファイル | 章 |
|---|---|
| `01-purpose.md` | §1 何のためのツールか、§2 役割と原則、§3 名前 |
| `02-storage-and-git.md` | §4 配置とgit（`.mtqg/`の探し方と作り方を含む） |
| `03-data-model.md` | §5 データモデル（記録者、形式の契約、形式のバージョン）、§6 ID設計（ローカルの状態を含む） |
| `04-append-and-merge.md` | §7 追記とマージ（書き出し規則、unionマージ、表示の順序）、§8 削除と機密情報 |
| `05-reading.md` | §9 表示と振り返り（アーカイブを含む） |
| `06-implementation.md` | §10 実装方針（言語、コアの構成、表示の言語、文書とリポジトリの構成、テスト、配布） |
| `07-integrations.md` | §11 外部ツール連携（MCP、フック、VSCode拡張） |
| `08-development.md` | §12 開発の進め方、§13 先行事例 |
| `cli.md` | CLI設計（旧「CLI検討案」、CLI設計§1〜§10） |
| `history.md` | 改訂履歴（決めたことの時系列） |

旧「構想メモ」§14（未検討・今後詰める事項）は `PLAN.md` に移した。
